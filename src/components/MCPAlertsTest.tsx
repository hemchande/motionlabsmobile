/**
 * MCP Alerts Test — re-exports AlertDashboard which uses athleteCoachFastApiClient.
 * Kept for backward compatibility. Use AlertDashboard directly for new code.
 */

export { AlertDashboard as MCPAlertsTest } from './AlertDashboard';
